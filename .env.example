# Token de autenticacao — gerado automaticamente pelo instalador
BRIDGE_TOKEN=cole_o_token_aqui

# SQL Server local (na maquina do cliente)
DB_HOST=localhost
DB_PORT=1433
DB_NAME=<nome_do_banco>
DB_USER=lc_dashboard
DB_PASS=Max@1225

# Porta da bridge (exposta pelo Cloudflare Tunnel)
PORT=3055
